const SectionWrapper = ({ children }) => {
    return (
        <section className="mt-[100px] mb-[40px] flex w-full items-center justify-center">
            {children}
        </section>
    )
}

export default SectionWrapper
